<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Sandra Molina | #1 Toyota Expert in South Florida | Toyota of Hollywood</title>
  <meta name="description" content="26+ years. 20,000+ South Florida families. Sandra Molina is the #1 Toyota Pro in the Southeast USA — and she works directly with you. Toyota of Hollywood, FL."/>
  <meta name="keywords" content="Toyota Hollywood FL, Toyota expert South Florida, best Toyota dealer Broward County, Sandra Molina Toyota, buy Toyota South Florida, Toyota fleet manager"/>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&family=DM+Sans:wght@300;400;500;600&display=swap" rel="stylesheet"/>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    :root {
      --red: #C8102E;
      --red-dark: #a00d24;
      --dark: #0f0f0f;
      --grey-dark: #1a1a1a;
      --grey-mid: #555555;
      --grey-light: #e8e8e8;
      --grey-bg: #f4f4f4;
      --white: #ffffff;
      --text: #1a1a1a;
    }
    html { scroll-behavior: smooth; }
    body { font-family: 'DM Sans', sans-serif; background: var(--white); color: var(--text); overflow-x: hidden; }

    /* ═══ NAV ═══ */
    nav {
      position: fixed; top: 0; left: 0; right: 0; z-index: 100;
      background: rgba(15,15,15,0.97); backdrop-filter: blur(10px);
      height: 66px; padding: 0 64px;
      display: flex; align-items: center; justify-content: space-between;
      border-bottom: 1px solid rgba(255,255,255,0.05);
    }
    .nav-logo { font-family: 'Cormorant Garamond', serif; font-size: 24px; font-weight: 400; color: var(--white); text-decoration: none; letter-spacing: 0.04em; }
    .nav-logo span { color: var(--red); }
    .nav-right { display: flex; align-items: center; gap: 36px; }
    .nav-right a { font-size: 12px; font-weight: 400; letter-spacing: 0.12em; text-transform: uppercase; color: rgba(255,255,255,0.5); text-decoration: none; transition: color 0.3s; }
    .nav-right a:hover { color: white; }
    .nav-cta { padding: 10px 30px; background: var(--red) !important; color: white !important; font-weight: 500 !important; transition: background 0.3s !important; }
    .nav-cta:hover { background: var(--red-dark) !important; }

    /* ═══ HERO ═══ */
    /* Desktop: 50/50 grid. Text+stats left, slideshow right. Viewport height. */
    .hero-wrapper {
      height: 100vh; height: 100svh;
      display: flex; flex-direction: column;
      padding-top: 66px;
      background: var(--grey-dark);
      position: relative; overflow: hidden;
    }
    .hero-wrapper::before {
      content: ''; position: absolute; inset: 0; pointer-events: none; z-index: 1;
      background: radial-gradient(ellipse at 80% 40%, rgba(200,16,46,0.10) 0%, transparent 55%);
    }
    .hero-main {
      flex: 1; min-height: 0;
      display: grid; grid-template-columns: 1fr 1fr;
      position: relative; z-index: 2;
    }
    .hero-left {
      display: flex; flex-direction: column; justify-content: center;
      padding: 40px 56px 40px 80px;
    }
    .expertise-badge {
      display: inline-flex; align-items: center; gap: 12px; margin-bottom: 32px;
      opacity: 0; animation: fadeUp 0.7s ease 0.1s forwards;
    }
    .badge-line { width: 36px; height: 2px; background: var(--red); }
    .badge-copy { font-size: 11px; font-weight: 500; letter-spacing: 0.25em; text-transform: uppercase; color: var(--red); }
    .hero-headline {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(44px, 5vw, 78px); font-weight: 300; line-height: 1.06;
      color: var(--white); margin-bottom: 24px;
      opacity: 0; animation: fadeUp 0.7s ease 0.25s forwards;
    }
    .hero-headline em { font-style: italic; color: var(--red); }
    .hero-desc {
      font-size: 17px; font-weight: 300; line-height: 1.75;
      color: rgba(255,255,255,0.52); max-width: 460px; margin-bottom: 40px;
      opacity: 0; animation: fadeUp 0.7s ease 0.4s forwards;
    }
    .hero-desc strong { color: rgba(255,255,255,0.9); font-weight: 400; }
    .hero-actions {
      display: flex; align-items: center; gap: 16px;
      opacity: 0; animation: fadeUp 0.7s ease 0.52s forwards;
    }
    .btn-red {
      display: inline-block; padding: 15px 42px; background: var(--red); color: white;
      font-size: 12px; font-weight: 500; letter-spacing: 0.18em; text-transform: uppercase;
      text-decoration: none; transition: background 0.3s, transform 0.2s, box-shadow 0.3s;
    }
    .btn-red:hover { background: var(--red-dark); transform: translateY(-2px); box-shadow: 0 8px 24px rgba(200,16,46,0.3); }
    .btn-outline {
      display: inline-block; padding: 14px 36px; border: 1px solid rgba(255,255,255,0.18);
      color: rgba(255,255,255,0.65); font-size: 12px; font-weight: 400; letter-spacing: 0.14em;
      text-transform: uppercase; text-decoration: none; transition: all 0.3s;
    }
    .btn-outline:hover { border-color: rgba(255,255,255,0.55); color: white; }

    /* Right: slideshow */
    .hero-right { position: relative; overflow: hidden; opacity: 0; animation: fadeIn 1s ease 0.3s forwards; }
    .hero-right::before {
      content: ''; position: absolute; left: 0; top: 0; bottom: 0; width: 80px;
      background: linear-gradient(to right, var(--grey-dark), transparent); z-index: 3;
    }
    .hero-slideshow { position: relative; width: 100%; height: 100%; }
    .hero-slideshow img {
      position: absolute; inset: 0; width: 100%; height: 100%;
      object-fit: cover; object-position: center top;
      opacity: 0; transition: opacity 1.2s ease-in-out;
    }
    .hero-slideshow img.active { opacity: 1; }
    .hero-dots {
      position: absolute; bottom: 24px; left: 50%; transform: translateX(-50%);
      z-index: 4; display: flex; gap: 10px;
      opacity: 0; animation: fadeIn 1s ease 0.8s forwards;
    }
    .hero-dot {
      width: 10px; height: 10px; border-radius: 50%; border: 2px solid rgba(255,255,255,0.3);
      background: transparent; cursor: pointer; transition: all 0.3s; padding: 0;
    }
    .hero-dot.active { background: var(--red); border-color: var(--red); transform: scale(1.2); }

    /* Stats bar: bottom of hero-wrapper, always visible */
    .stats-bar {
      flex-shrink: 0; position: relative; z-index: 2;
      display: flex; border-top: 1px solid rgba(255,255,255,0.08);
      padding: 0 80px; background: var(--grey-dark);
      opacity: 0; animation: fadeUp 0.6s ease 0.7s forwards;
    }
    .hero-stat {
      flex: 1; padding: 20px 24px 22px 0;
      border-right: 1px solid rgba(255,255,255,0.06); margin-right: 24px;
    }
    .hero-stat:last-child { border-right: none; margin-right: 0; padding-right: 0; }
    .stat-val { font-family: 'Cormorant Garamond', serif; font-size: 34px; font-weight: 600; color: var(--white); line-height: 1; display: block; }
    .stat-lbl { font-size: 10px; letter-spacing: 0.12em; text-transform: uppercase; color: rgba(255,255,255,0.3); margin-top: 5px; display: block; }

    /* ═══ RED DIVIDER ═══ */
    .red-divider { height: 4px; background: var(--red); }

    /* ═══ STORY ═══ */
    .story-section { display: grid; grid-template-columns: 1fr 1fr; min-height: 680px; }
    .story-image { position: relative; overflow: hidden; background: var(--grey-light); order: -1; }
    .story-image img { width: 100%; height: 100%; object-fit: cover; object-position: center center; transition: transform 8s ease-out; }
    .story-image:hover img { transform: scale(1.05); }
    .story-image::after { content: ''; position: absolute; bottom: 0; left: 0; width: 100%; height: 4px; background: var(--red); z-index: 2; }
    .story-content { background: var(--white); padding: 80px 72px; display: flex; flex-direction: column; justify-content: center; }
    .eyebrow { font-size: 10px; font-weight: 600; letter-spacing: 0.28em; text-transform: uppercase; color: var(--red); margin-bottom: 20px; display: block; }
    .story-content h2 { font-family: 'Cormorant Garamond', serif; font-size: clamp(32px, 3.5vw, 50px); font-weight: 300; line-height: 1.15; color: var(--text); margin-bottom: 28px; }
    .story-content h2 em { font-style: italic; color: var(--red); }
    .story-content p { font-size: 16px; font-weight: 300; line-height: 1.85; color: #3a3a3a; margin-bottom: 20px; }
    .story-content p strong { font-weight: 500; color: var(--text); }
    .story-creds { display: flex; flex-wrap: wrap; gap: 10px; margin-top: 32px; margin-bottom: 36px; }
    .cred-tag { padding: 7px 14px; background: var(--grey-bg); border-left: 2px solid var(--red); font-size: 11px; font-weight: 400; color: var(--grey-mid); }

    /* ═══ PROCESS ═══ */
    .process-section { background: var(--grey-dark); padding: 100px 80px; position: relative; overflow: hidden; }
    .process-section::before {
      content: ''; position: absolute; top: -200px; right: -200px; width: 600px; height: 600px;
      background: radial-gradient(circle, rgba(200,16,46,0.06) 0%, transparent 70%); pointer-events: none;
    }
    .process-header { max-width: 680px; margin-bottom: 64px; }
    .process-header h2 { font-family: 'Cormorant Garamond', serif; font-size: clamp(34px, 4vw, 54px); font-weight: 300; line-height: 1.15; color: var(--white); margin-bottom: 20px; }
    .process-header h2 em { font-style: italic; color: var(--red); }
    .process-header p { font-size: 16px; font-weight: 300; line-height: 1.8; color: rgba(255,255,255,0.45); max-width: 520px; }
    .process-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2px; }
    .process-row-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 2px; margin-top: 2px; }
    .p-card {
      background: #222; padding: 40px 32px 44px; position: relative; transition: background 0.3s;
    }
    .p-card:hover { background: #282828; }
    .p-card::before {
      content: ''; position: absolute; top: 0; left: 0; right: 0; height: 3px;
      background: var(--red); transform: scaleX(0); transform-origin: left; transition: transform 0.4s ease;
    }
    .p-card:hover::before { transform: scaleX(1); }
    .p-card .step-num {
      font-family: 'Cormorant Garamond', serif; font-size: 48px; font-weight: 300;
      color: rgba(200,16,46,0.2); line-height: 1; margin-bottom: 20px;
    }
    .p-card h3 {
      font-family: 'Cormorant Garamond', serif; font-size: 22px; font-weight: 400;
      color: var(--white); line-height: 1.3; margin-bottom: 12px;
    }
    .p-card p { font-size: 14px; font-weight: 300; line-height: 1.75; color: rgba(255,255,255,0.45); }

    /* ═══ TESTIMONIALS ═══ */
    .testimonials-section { background: var(--white); padding: 100px 80px; }
    .testimonials-header { max-width: 600px; margin-bottom: 56px; }
    .testimonials-header h2 { font-family: 'Cormorant Garamond', serif; font-size: clamp(34px, 4vw, 52px); font-weight: 300; line-height: 1.15; color: var(--text); margin-bottom: 16px; }
    .testimonials-header h2 em { font-style: italic; color: var(--red); }
    .testimonials-header p { font-size: 16px; font-weight: 300; line-height: 1.8; color: var(--grey-mid); }
    .testimonials-grid { display: grid; grid-template-columns: 1.15fr 0.85fr; grid-template-rows: auto auto; gap: 2px; }
    .t-card {
      background: var(--grey-bg); padding: 40px 36px; transition: all 0.35s;
      position: relative; overflow: hidden;
    }
    .t-card::before {
      content: '\201C'; position: absolute; top: -10px; right: 20px;
      font-family: 'Cormorant Garamond', serif; font-size: 160px; font-weight: 600;
      color: rgba(200,16,46,0.04); line-height: 1; pointer-events: none;
    }
    .t-card:hover { background: var(--grey-light); transform: translateY(-2px); box-shadow: 0 8px 32px rgba(0,0,0,0.06); }
    .t-card:first-child { grid-row: 1 / 3; display: flex; flex-direction: column; justify-content: center; }
    .t-card:first-child blockquote { font-size: 24px; }
    .t-stars { color: var(--red); font-size: 13px; letter-spacing: 3px; margin-bottom: 22px; }
    .t-card blockquote { font-family: 'Cormorant Garamond', serif; font-size: 20px; font-style: italic; font-weight: 300; line-height: 1.6; color: var(--text); margin-bottom: 28px; }
    .t-divider { width: 32px; height: 2px; background: var(--red); margin-bottom: 16px; }
    .t-name { font-size: 13px; font-weight: 500; color: var(--text); }
    .t-detail { font-size: 11px; color: var(--grey-mid); margin-top: 4px; }

    /* ═══ CTA ═══ */
    .cta-section {
      background: var(--red); padding: 100px 80px; position: relative; overflow: hidden;
      display: flex; align-items: center; justify-content: center; text-align: center;
    }
    .cta-section::before {
      content: ''; position: absolute; inset: 0; pointer-events: none;
      background: radial-gradient(ellipse at 30% 50%, rgba(255,255,255,0.08) 0%, transparent 60%);
    }
    .cta-inner { position: relative; z-index: 2; max-width: 680px; }
    .cta-section h2 { font-family: 'Cormorant Garamond', serif; font-size: clamp(36px, 4.5vw, 64px); font-weight: 300; line-height: 1.1; color: white; margin-bottom: 18px; }
    .cta-section h2 em { font-style: italic; }
    .cta-section p { font-size: 17px; font-weight: 300; color: rgba(255,255,255,0.72); margin-bottom: 40px; line-height: 1.65; }
    .cta-btns { display: flex; justify-content: center; gap: 16px; flex-wrap: wrap; }
    .btn-white {
      display: inline-block; padding: 17px 48px; background: white; color: var(--red);
      font-size: 12px; font-weight: 600; letter-spacing: 0.18em; text-transform: uppercase;
      text-decoration: none; transition: all 0.3s;
    }
    .btn-white:hover { background: var(--dark); color: white; transform: translateY(-2px); box-shadow: 0 8px 24px rgba(0,0,0,0.3); }
    .btn-white-outline {
      display: inline-block; padding: 16px 44px; border: 1px solid rgba(255,255,255,0.38);
      color: rgba(255,255,255,0.85); font-size: 12px; font-weight: 400; letter-spacing: 0.15em;
      text-transform: uppercase; text-decoration: none; transition: all 0.3s;
    }
    .btn-white-outline:hover { border-color: white; color: white; }

    /* ═══ FOOTER ═══ */
    footer { background: var(--dark); padding: 60px 80px 32px; }
    .footer-top { display: grid; grid-template-columns: 1.4fr 1fr 1fr 1fr; gap: 60px; padding-bottom: 48px; border-bottom: 1px solid rgba(255,255,255,0.07); margin-bottom: 28px; }
    .footer-logo { font-family: 'Cormorant Garamond', serif; font-size: 26px; color: white; text-decoration: none; display: block; margin-bottom: 14px; }
    .footer-logo span { color: var(--red); }
    .footer-desc { font-size: 13px; font-weight: 300; line-height: 1.75; color: rgba(255,255,255,0.3); }
    .footer-col-head { font-size: 10px; font-weight: 600; letter-spacing: 0.25em; text-transform: uppercase; color: rgba(255,255,255,0.25); margin-bottom: 20px; }
    .footer-links { list-style: none; }
    .footer-links li { margin-bottom: 10px; }
    .footer-links a { font-size: 14px; font-weight: 300; color: rgba(255,255,255,0.45); text-decoration: none; transition: color 0.3s; }
    .footer-links a:hover { color: white; }
    .footer-contact-line { font-size: 14px; font-weight: 300; color: rgba(255,255,255,0.45); line-height: 1.9; }
    .footer-contact-line a { color: var(--red); text-decoration: none; }
    .footer-bottom { display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 12px; }
    .footer-bottom p, .footer-bottom a { font-size: 12px; color: rgba(255,255,255,0.18); text-decoration: none; }
    .footer-bottom a:hover { color: rgba(255,255,255,0.5); }
    .footer-legal { display: flex; gap: 20px; }

    /* ═══ SCROLL REVEAL ═══ */
    .reveal { opacity: 0; transform: translateY(28px); transition: opacity 0.7s ease, transform 0.7s ease; }
    .reveal.visible { opacity: 1; transform: translateY(0); }
    .reveal-d1 { transition-delay: 0.1s; }
    .reveal-d2 { transition-delay: 0.2s; }
    .reveal-d3 { transition-delay: 0.3s; }

    /* ═══ ANIMATIONS ═══ */
    @keyframes fadeUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
    @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }

    /* ═══ CHAT WIDGET ═══ */
    .story-content, .testimonials-section, .cta-inner { padding-right: 80px; }

    /* ═══ TABLET ═══ */
    @media (max-width: 1024px) {
      nav { padding: 0 28px; }
      .nav-right a:not(.nav-cta) { display: none; }
      .hero-main { grid-template-columns: 1fr; }
      .hero-left { padding: 28px 28px 20px; }
      .hero-headline { font-size: clamp(32px, 5.5vw, 46px); margin-bottom: 14px; }
      .hero-desc { font-size: 14px; margin-bottom: 20px; }
      .hero-actions { margin-bottom: 0; }
      .hero-right { min-height: 260px; }
      .hero-right::before { display: none; }
      .hero-dots { bottom: 14px; }
      .stats-bar { padding: 0 28px; flex-wrap: wrap; }
      .hero-stat { flex: 1 1 40%; padding: 14px 16px 14px 0; margin-right: 0; border-right: none; }
      .stat-val { font-size: 28px; }
      .story-section { grid-template-columns: 1fr; }
      .story-image { height: 380px; order: 0; }
      .story-content { padding: 60px 28px; }
      .process-section { padding: 72px 28px; }
      .process-grid { grid-template-columns: 1fr; }
      .process-row-2 { grid-template-columns: 1fr; }
      .testimonials-section { padding: 72px 28px; }
      .testimonials-grid { grid-template-columns: 1fr; }
      .t-card:first-child { grid-row: auto; }
      .t-card:first-child blockquote { font-size: 20px; }
      .cta-section { padding: 72px 28px; }
      .cta-btns { flex-direction: column; align-items: center; }
      footer { padding: 48px 28px 24px; }
      .footer-top { grid-template-columns: 1fr; gap: 36px; }
      .story-content, .testimonials-section, .cta-inner { padding-right: 28px; }
    }

    /* ═══ MOBILE ═══ */
    @media (max-width: 600px) {
      .hero-left { padding: 16px 20px 14px; }
      .expertise-badge { margin-bottom: 10px; }
      .badge-copy { font-size: 9px; }
      .hero-headline { font-size: 26px; margin-bottom: 8px; }
      .hero-desc { font-size: 12px; line-height: 1.5; margin-bottom: 14px; }
      .hero-actions { gap: 8px; }
      .btn-red { padding: 11px 20px; font-size: 10px; letter-spacing: 0.12em; }
      .btn-outline { padding: 10px 16px; font-size: 10px; letter-spacing: 0.1em; }
      .hero-right { min-height: 200px; }
      .hero-dots { bottom: 8px; gap: 6px; }
      .hero-dot { width: 7px; height: 7px; }
      .stats-bar { padding: 0 20px; }
      .hero-stat { padding: 8px 8px 8px 0; }
      .stat-val { font-size: 22px; }
      .stat-lbl { font-size: 8px; }
      .story-image { height: 260px; }
      .story-content { padding: 36px 20px; }
      .story-content h2 { font-size: 26px; }
      .process-section { padding: 56px 20px; }
      .p-card h3 { font-size: 20px; }
      .testimonials-section { padding: 56px 20px; }
      .cta-section { padding: 56px 20px; }
      .cta-section h2 { font-size: 30px; }
      footer { padding: 40px 20px 24px; }
    }
  </style>
</head>
<body>

  <!-- NAV -->
  <nav>
    <a href="#" class="nav-logo">Sandra<span>Toyota</span></a>
    <div class="nav-right">
      <a href="https://sandratoyota.com/about-sandra">About Sandra</a>
      <a href="#process">How It Works</a>
      <a href="tel:+17863056671">(786) 305-6671</a>
      <a href="https://link.sandratoyota.com/widget/booking/7cklGURXmfVziv8H7N91" class="nav-cta">Schedule a Visit</a>
    </div>
  </nav>

  <!-- ═══ HERO ═══ -->
  <div class="hero-wrapper">
    <div class="hero-main">
      <div class="hero-left">
        <div>
          <div class="expertise-badge">
            <span class="badge-line"></span>
            <span class="badge-copy">26 Years of Toyota Expertise · Hollywood, FL</span>
          </div>
          <h1 class="hero-headline">South Florida's<br>Most Trusted<br><em>Toyota Expert.</em></h1>
          <p class="hero-desc"><strong>Sandra Molina</strong> is the <strong>#1 Toyota Pro in the Southeast United States</strong> — and she works directly with you. No middlemen, no pressure, no guessing. Just 26 years of expertise working in your favor.</p>
          <div class="hero-actions">
            <a href="https://link.sandratoyota.com/widget/booking/7cklGURXmfVziv8H7N91" class="btn-red">Schedule Your Visit</a>
            <a href="tel:+17863056671" class="btn-outline">Call Sandra Directly</a>
          </div>
        </div>
      </div>
      <div class="hero-right">
        <div class="hero-slideshow" id="heroSlideshow">
          <img src="https://assets.cdn.filesafe.space/g4GYB6d9iPbCV6PQdlCd/media/695d54bea8a91cc42773034c.png" alt="Sandra with happy client and their new Toyota" class="active" />
          <img src="https://assets.cdn.filesafe.space/g4GYB6d9iPbCV6PQdlCd/media/695d54bea88e87368cbfa9a3.png" alt="Sandra with happy client and their new Toyota" />
          <img src="https://assets.cdn.filesafe.space/g4GYB6d9iPbCV6PQdlCd/media/695d54be6c1415a06c629b36.png" alt="Sandra with happy client and their new Toyota" />
          <img src="https://assets.cdn.filesafe.space/g4GYB6d9iPbCV6PQdlCd/media/695d54bea8a91c090f73034a.png" alt="Sandra with happy client and their new Toyota" />
          <img src="https://assets.cdn.filesafe.space/g4GYB6d9iPbCV6PQdlCd/media/695d54bec5c3f87cde89f3bd.png" alt="Sandra with happy client and their new Toyota" />
          <img src="https://assets.cdn.filesafe.space/g4GYB6d9iPbCV6PQdlCd/media/695d54be94b57d67b3bb7434.png" alt="Sandra with happy client and their new Toyota" />
          <img src="https://assets.cdn.filesafe.space/g4GYB6d9iPbCV6PQdlCd/media/695d54bea8a91c3b5473034d.png" alt="Sandra with happy client and their new Toyota" />
        </div>
        <div class="hero-dots" id="heroDots"></div>
      </div>
    </div>
    <div class="stats-bar">
      <div class="hero-stat">
        <span class="stat-val">26+</span>
        <span class="stat-lbl">Years at Toyota of Hollywood</span>
      </div>
      <div class="hero-stat">
        <span class="stat-val">20K+</span>
        <span class="stat-lbl">South Florida Clients</span>
      </div>
      <div class="hero-stat">
        <span class="stat-val">#1</span>
        <span class="stat-lbl">Southeast United States</span>
      </div>
      <div class="hero-stat">
        <span class="stat-val">Top&nbsp;50</span>
        <span class="stat-lbl">Toyota Pros, Nationwide</span>
      </div>
    </div>
  </div>

  <div class="red-divider"></div>

  <!-- ═══ STORY ═══ -->
  <section class="story-section" id="story">
    <div class="story-content">
      <span class="eyebrow reveal">Sandra's Story</span>
      <h2 class="reveal reveal-d1">From Queens to South Florida —<br><em>A Career Built on Integrity.</em></h2>
      <p class="reveal reveal-d2">I was born in Queens, New York, but South Florida claimed me at 15. My parents came from the Dominican Republic, and it didn't take long for our family to figure out we were not built for cold weather. I grew up in Miami, married my childhood sweetheart, and raised two kids.</p>
      <p class="reveal reveal-d2">I joined Toyota of Hollywood in 1999 and stepped onto the sales floor at a time when the industry was almost entirely male-dominated. I kept my head down, treated every customer the way I'd want to be treated, and let the results speak for themselves.</p>
      <p class="reveal reveal-d2">For the majority of the past 20+ years, I've been the <strong>#1 Toyota salesperson at Toyota of Hollywood and across the entire Southeast United States.</strong> What I'm most proud of isn't a number — it's the thousands of people who trusted me enough to send their friends, their kids, and their parents back to me. That kind of loyalty comes from one thing: integrity.</p>
      <div class="story-creds reveal reveal-d3">
        <div class="cred-tag">#1 Southeast USA</div>
        <div class="cred-tag">Top 50 Toyota Pros</div>
        <div class="cred-tag">26+ Years at TOH</div>
        <div class="cred-tag">Craig Zinn Organization</div>
      </div>
      <a href="https://sandratoyota.com/about-sandra" class="btn-red reveal reveal-d3" style="display:inline-block;">Read Sandra's Full Story →</a>
    </div>
    <div class="story-image">
      <img src="https://assets.cdn.filesafe.space/g4GYB6d9iPbCV6PQdlCd/media/68c8635c4f724e239645b858.jpeg" alt="Sandra Molina and her team holding up a Toyota award" />
    </div>
  </section>

  <!-- ═══ PROCESS ═══ -->
  <section class="process-section" id="process">
    <div class="process-header reveal">
      <span class="eyebrow" style="color:var(--red);">How It Works</span>
      <h2>A Better Way to Buy Your Toyota — <em>Step by Step.</em></h2>
      <p>As Fleet Manager, Sandra has the authority to give you the real number from the very first conversation. No waiting rooms, no middlemen, no games.</p>
    </div>
    <div class="process-grid reveal">
      <div class="p-card"><div class="step-num">01</div><h3>We Talk About What You Need</h3><p>Not what's on special. I ask about your life — your family, your commute, your priorities — and tell you which Toyota genuinely fits.</p></div>
      <div class="p-card"><div class="step-num">02</div><h3>I Verify Real Inventory</h3><p>I physically confirm the car is on the lot before you move. No bait-and-switch. You'll know the exact vehicle or its factory arrival date.</p></div>
      <div class="p-card"><div class="step-num">03</div><h3>You Get the Real Number First</h3><p>Taxes, fees, everything — sent to you clearly before your visit. Review it at home, on your own time. No surprises.</p></div>
    </div>
    <div class="process-row-2 reveal reveal-d1">
      <div class="p-card"><div class="step-num">04</div><h3>I Am the Manager</h3><p>You never wait while someone "checks with the manager." As Fleet Manager, I have the authority to close from the very first call.</p></div>
      <div class="p-card"><div class="step-num">05</div><h3>You Drive Home Happy</h3><p>Because everything is handled upfront, your visit is simple — test drive, sign, and go. No pressure, no drama, no regrets.</p></div>
    </div>
  </section>

  <!-- ═══ TESTIMONIALS ═══ -->
  <section class="testimonials-section" id="testimonials">
    <div class="testimonials-header reveal">
      <span class="eyebrow">Client Stories</span>
      <h2>20,000+ South Florida Families.<br><em>One Name They Trust.</em></h2>
      <p>Sandra's clients don't just come back — they send everyone they love. These are the stories that built a 26-year career.</p>
    </div>
    <div class="testimonials-grid">
      <div class="t-card reveal">
        <div class="t-stars">★★★★★</div>
        <blockquote>"Sandra gave me the full out-the-door price before I even left my house. I drove in, test drove, signed, and was home in two hours. I've never had an experience like it at any dealership."</blockquote>
        <div class="t-divider"></div>
        <div class="t-name">Maria R.</div>
        <div class="t-detail">2024 Toyota RAV4 · Pembroke Pines, FL</div>
      </div>
      <div class="t-card reveal reveal-d1">
        <div class="t-stars">★★★★★</div>
        <blockquote>"My father bought from Sandra. Then my sister. Then me. She never once tried to upsell us or hide anything. She's genuinely honest — and that is rare in this business."</blockquote>
        <div class="t-divider"></div>
        <div class="t-name">Carlos M.</div>
        <div class="t-detail">2025 Toyota Camry Hybrid · Miramar, FL</div>
      </div>
      <div class="t-card reveal reveal-d2">
        <div class="t-stars">★★★★★</div>
        <blockquote>"Four cars over the years. She remembers my name, knows my family, and always comes through. There is no one else I would trust with a purchase this large."</blockquote>
        <div class="t-divider"></div>
        <div class="t-name">Jennifer T.</div>
        <div class="t-detail">2025 Toyota Highlander · Fort Lauderdale, FL</div>
      </div>
    </div>
  </section>

  <!-- ═══ CTA ═══ -->
  <section class="cta-section">
    <div class="cta-inner reveal">
      <h2>Ready to buy your Toyota<br>the <em>right way?</em></h2>
      <p>No pressure. No games. Just the car you want, at a price that's clear from the very start. Schedule your visit with Sandra today.</p>
      <div class="cta-btns">
        <a href="https://link.sandratoyota.com/widget/booking/7cklGURXmfVziv8H7N91" class="btn-white">Schedule Your VIP Visit</a>
        <a href="tel:+17863056671" class="btn-white-outline">Call (786) 305-6671</a>
      </div>
    </div>
  </section>

  <!-- ═══ FOOTER ═══ -->
  <footer>
    <div class="footer-top">
      <div>
        <a href="#" class="footer-logo">Sandra<span>Toyota</span></a>
        <p class="footer-desc">The #1 Toyota Pro in the Southeast USA. Serving South Florida with transparency, integrity, and 26+ years of genuine expertise at Toyota of Hollywood.</p>
      </div>
      <div>
        <div class="footer-col-head">Quick Links</div>
        <ul class="footer-links">
          <li><a href="https://sandratoyota.com/about-sandra">About Sandra</a></li>
          <li><a href="#process">How It Works</a></li>
          <li><a href="#testimonials">Client Stories</a></li>
          <li><a href="https://link.sandratoyota.com/widget/booking/7cklGURXmfVziv8H7N91">Schedule a Visit</a></li>
        </ul>
      </div>
      <div>
        <div class="footer-col-head">Contact</div>
        <p class="footer-contact-line">
          Toyota of Hollywood<br>1841 N State Rd 7<br>Hollywood, FL 33021<br><br>
          <a href="tel:+17863056671">(786) 305-6671</a>
        </p>
      </div>
      <div>
        <div class="footer-col-head">Areas We Serve</div>
        <ul class="footer-links">
          <li><a href="https://sandratoyota.com/toyota-dealer-hollywood-fl">Hollywood, FL</a></li>
          <li><a href="https://sandratoyota.com/toyota-dealer-pembroke-pines-fl">Pembroke Pines, FL</a></li>
          <li><a href="https://sandratoyota.com/toyota-dealer-miramar-fl">Miramar, FL</a></li>
          <li><a href="https://sandratoyota.com/toyota-dealer-davie-cooper-city-fl">Davie &amp; Cooper City, FL</a></li>
        </ul>
      </div>
    </div>
    <div class="footer-bottom">
      <p>&copy; 2026 Sandra Toyota's Team &middot; All Rights Reserved</p>
      <div class="footer-legal">
        <a href="https://sandratoyota.com/privacy-policy">Privacy Policy</a>
        <a href="https://sandratoyota.com/terms-and-conditions">Terms &amp; Conditions</a>
      </div>
    </div>
  </footer>

  <!-- ═══ SCRIPTS ═══ -->
  <script>
    // Slideshow
    (function() {
      var imgs = document.querySelectorAll('#heroSlideshow img');
      var dotsWrap = document.getElementById('heroDots');
      var current = 0, total = imgs.length;
      imgs.forEach(function(_, i) {
        var d = document.createElement('button');
        d.className = 'hero-dot' + (i === 0 ? ' active' : '');
        d.addEventListener('click', function() { goTo(i); });
        dotsWrap.appendChild(d);
      });
      var dots = dotsWrap.querySelectorAll('.hero-dot');
      function goTo(idx) {
        imgs[current].classList.remove('active');
        dots[current].classList.remove('active');
        current = idx;
        imgs[current].classList.add('active');
        dots[current].classList.add('active');
      }
      setInterval(function() { goTo((current + 1) % total); }, 4000);
    })();
    // Scroll reveal
    (function() {
      var els = document.querySelectorAll('.reveal');
      var obs = new IntersectionObserver(function(entries) {
        entries.forEach(function(e) { if (e.isIntersecting) e.target.classList.add('visible'); });
      }, { threshold: 0.15, rootMargin: '0px 0px -40px 0px' });
      els.forEach(function(el) { obs.observe(el); });
    })();
  </script>

  <script
    src="https://beta.leadconnectorhq.com/loader.js"
    data-resources-url="https://beta.leadconnectorhq.com/chat-widget/loader.js"
    data-widget-id="67f423c8a7c17b39868998ec">
  </script>

</body>
</html>
